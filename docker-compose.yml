version: "3.9"

services:
  server:
    build:
      context: ./server
      dockerfile: Dockerfile
    container_name: app-server
    restart: always
    ports:
      - "3001:3001"
    environment:
      - NODE_ENV=production
      - DATABASE_URL=postgresql://admin:1437@postgres:5432/maindb

  client:
    build:
      context: ./client
      dockerfile: Dockerfile
    container_name: app-client
    restart: always
    ports:
      - "3000:80"
    depends_on:
      - server