server {
  listen 80;

  server_name localhost;

  root /usr/share/nginx/html;

  index index.html;

  location / {
    try_files $uri $uri/ /index.html;
  }

  # если API идёт на server контейнер
  location /api/ {
    proxy_pass http://server:5000/;
  }
}